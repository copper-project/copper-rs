[package]
name = "v4lsrc"
version = "0.1.0"
edition = "2021"

[package.metadata]
copper_plugin_type = "driver"

[dependencies]
copper = { path = "../../copper" }
serde = { version = "1.0.202", features = ["derive"] }
linux-video = { version = "0.1.1" }