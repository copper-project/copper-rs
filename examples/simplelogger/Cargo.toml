[package]
name = "simplelogger"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
keywords = ["robotics", "middleware", "copper", "real-time"]

[dependencies]
copper = { path = "../../copper" }
v4lsrc = { path = "../v4lsrc" }
serde = { version = "1.0.202", features = ["derive"] }