[package]
name = "cu-rp-balancebot"
description = "This is a full robot example for the Copper project. It runs on the Raspberry Pi with the balance bot hat to balance a rod."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
homepage.workspace = true
repository.workspace = true
default-run = "balancebot-sim"

[package.metadata.cargo-shear]
ignored = ["serde"]

[dependencies]
serde = { workspace = true }
bevy_ecs = { version = "0.18.0", default-features = false }
cu-ads7883-new = { path = "../../components/sources/cu_ads7883", version = "0.15.0" }
cu-rp-sn754410-new = { path = "../../components/sinks/cu_rp_sn754410", version = "0.15.0" }
cu-rp-encoder = { path = "../../components/sources/cu_rp_encoder", version = "0.15.0" }
cu-pid = { path = "../../components/tasks/cu_pid", version = "0.15.0" }

# Log reader depencies
cu29-export = { workspace = true, optional = true, features = [
  "python",
  "mcap",
] }

avian3d = { workspace = true, default-features = false, features = [
  "bevy_scene",
  "collider-from-mesh",
  "debug-plugin",
  "parallel",
  "f32",
  "3d",
  "parry-f32",
  "xpbd_joints",
], optional = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
cu29 = { workspace = true }
cu-consolemon = { path = "../../components/monitors/cu_consolemon", version = "0.15.0" }
cu-bevymon = { path = "../../components/monitors/cu_bevymon", version = "0.15.0", optional = true }
image = { workspace = true, default-features = false, features = [
  "png",
], optional = true }
bevy = { workspace = true, default-features = false, features = [
  "x11",
  "wayland",
  "default_font",
  "bevy_render",
  "bevy_window",
  "bevy_core_pipeline",
  "bevy_pbr",
  "bevy_scene",
  "bevy_sprite",
  "bevy_gltf",
  "gltf_animation",
  "bevy_picking",
  "mesh_picking",
  "tonemapping_luts",
  "bevy_ui",
  "bevy_ui_render",
  "ktx2",
  "jpeg",
  "png",
  "zstd_rust",
  "trace",
  "reflect_auto_register",
], optional = true }
cached-path = { workspace = true, features = [
  "progress-bar",
  "rustls-tls",
], optional = true }
winit = { version = "=0.30.12", default-features = false, optional = true }
bevy_anti_alias = { version = "0.18.0", optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
cu29 = { path = "../../core/cu29", version = "0.15.0", default-features = false, features = [
  "std",
  "textlogs",
  "units",
] }
cu-bevymon = { path = "../../components/monitors/cu_bevymon", version = "0.15.0", default-features = false, optional = true }
bevy_anti_alias = { version = "0.18.0", optional = true }
bevy = { workspace = true, default-features = false, features = [
  "default_font",
  "bevy_render",
  "bevy_window",
  "bevy_core_pipeline",
  "bevy_pbr",
  "bevy_scene",
  "bevy_sprite",
  "bevy_gltf",
  "gltf_animation",
  "bevy_picking",
  "mesh_picking",
  "tonemapping_luts",
  "bevy_ui",
  "bevy_ui_render",
  "ktx2",
  "jpeg",
  "png",
  "zstd_rust",
  "reflect_auto_register",
], optional = true }

[features]
default = ["logreader", "sim"]
# generates an executable to read the logs
logreader = ["dep:cu29-export"]
sim_core = ["cu29/sysclock-perf"]
# dependencies to run the Bevy-based balancebot sim
sim = [
  "sim_core",
  "dep:bevy",
  "dep:bevy_anti_alias",
  "dep:avian3d",
  "dep:cached-path",
  "dep:image",
  "dep:winit",
]
sim-debug = ["logreader", "cu29/reflect", "cu29/remote-debug"]
bevymon = ["sim", "dep:cu-bevymon"]

[[bin]]
name = "balancebot"
path = "src/main.rs"

[[bin]]
name = "balancebot-logreader"
path = "src/logreader.rs"
required-features = ["logreader"]

[[bin]]
name = "balancebot-bevymon"
path = "src/bevymon.rs"
required-features = ["bevymon"]

[[bin]]
name = "balancebot-sim"
path = "src/sim.rs"
required-features = ["sim"]

[[bin]]
name = "balancebot-resim"
path = "src/resim.rs"
required-features = ["sim-debug"]
