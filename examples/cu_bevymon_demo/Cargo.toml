[package]
name = "cu-bevymon-demo"
description = "Minimal side-by-side Bevy sim and Copper monitor demo."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
homepage.workspace = true
repository.workspace = true
default-run = "cu-bevymon-demo"
publish = false

[dependencies]
bevy_ecs = { version = "0.18.0", default-features = false }
cu29 = { path = "../../core/cu29", version = "0.15.0", default-features = false, features = [
  "std",
  "textlogs",
] }
bincode = { workspace = true }
serde = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
bevy = { workspace = true, default-features = false, features = [
  "x11",
  "wayland",
  "default_font",
  "bevy_render",
  "bevy_window",
  "bevy_core_pipeline",
  "bevy_pbr",
  "bevy_ui",
  "bevy_ui_render",
] }
cu-bevymon = { path = "../../components/monitors/cu_bevymon", version = "0.15.0" }
image = { workspace = true, default-features = false, features = ["png"] }
winit = { version = "=0.30.12", default-features = false }

[target.'cfg(target_arch = "wasm32")'.dependencies]
bevy = { workspace = true, default-features = false, features = [
  "default_font",
  "bevy_render",
  "bevy_window",
  "bevy_core_pipeline",
  "bevy_pbr",
  "bevy_ui",
  "bevy_ui_render",
] }
cu-bevymon = { path = "../../components/monitors/cu_bevymon", version = "0.15.0", default-features = false }

[[bin]]
name = "cu-bevymon-demo"
path = "src/main.rs"
