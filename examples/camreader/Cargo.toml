[package]
name = "camreader"
version = "0.1.0"
edition = "2021"

[package.metadata]
copper_plugin_type = "driver"

[dependencies]
copper = { path = "../../copper" }
serde = { version = "1.0.201", features = ["derive"] }