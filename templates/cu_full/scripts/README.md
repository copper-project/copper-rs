Workspace automation and deployment helpers live here.

Common utilities:
- `log`: extract the structured log via the log reader.
- `cl`: extract CopperLists from the log output.
- `rcfg`: render the execution DAG from the app config.
